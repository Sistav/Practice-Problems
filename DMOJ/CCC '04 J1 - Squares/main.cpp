#include <iostream>
#include <math.h>
using namespace std;
int main() {
    int numero;
    cin >> numero;
    cout << "The largest square has side length " << (int) sqrt(numero) << "." << endl;
    return 0;
}
