#include <iostream>
using namespace std;
int main() {
    int q;
    cin >> q;
    for (int i = 0; i < q; ++i) {
        int min,max;
        cin >> min >> max;

        bool aaaaaaaa = 0;
        for (int i = begin; i < end; i = i+2)
        {
            isPrime = 1;
            for (int j = 2; j<i; j++)
                if (i % j == 0)
                {
                    aaaaaaaa = 0;
                    break;
                }
            if (aaaaaaaa)
                file << i << " \n";
        }
    }
}
